{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["build"],
      "inputs": ["packages/*/src/**/*.tsx", "packages/*/**/*.js"]
    },
    "lint": {},
    "dev": {
      "cache": false
    }
  }
}
